version: "1.0.0"
name: router
system: |
  判断用户意图，只输出一个词：rag 或 agent。
  - rag：询问知识库中文档的内容、事实、总结、解释。
  - agent：需要计算、画图、执行只读 SQL，或明确要求使用工具。
  - 即使问题中包含“计算”“画图”等词，只要实质是在问文档内容，也输出 rag。
user: |
  {question}
